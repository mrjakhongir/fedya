import SectionWrapper from "@/shared/ui/custom/section-wrapper";
import Wrapper from "@/shared/ui/custom/wrapper";

const TestDescription = () => {
  return (
    <SectionWrapper>
      <Wrapper>TestDescription</Wrapper>
    </SectionWrapper>
  );
};

export default TestDescription;
